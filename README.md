# Claude Code and Codex Configuration Examples

## Claude Code

- **CLAUDE.example.md** - Rename it and move to `~/CLAUDE.md`.
- **settings.example.json** - Rename it and move to `project/.claude/settings.json`.
- **.mcp.example.json** - Rename it and move to `project/.claude/.mcp.json`.

## Codex

- **config.example.toml** - Rename it and move to `project/.codex/config.toml`.

> For Codex, you can copy the `~/CLAUDE.md` to `~/.codex/AGENTS.md`